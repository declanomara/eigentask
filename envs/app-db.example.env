# Example env for the app Postgres instance (use on the server, not committed)
# Copy to envs/app-db.dev.env for local dev, or use as template for staging/prod.
POSTGRES_DB=eigentask
POSTGRES_USER=eigentask
POSTGRES_PASSWORD=change-me-strong
