# Server-side API calls: use host gateway so the web container can reach the API on the host's port 8000
API_ORIGIN=http://host.docker.internal:8000
PUBLIC_API_ORIGIN=http://localhost:8000
PUBLIC_APP_ORIGIN=http://localhost:3000
