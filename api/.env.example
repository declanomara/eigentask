# Example environment for API (copy to your server as /etc/eigentask/api.env)
# Do NOT commit real secrets.

# --- Origins ---
# In dev (compose): http://web:3000 or http://localhost:3000
# In prod: https://eigentask.com or https://app.eigentask.com
FRONTEND_ORIGIN=https://eigentask.com
# Public origin for the API itself (used in redirects/callbacks)
BACKEND_ORIGIN=https://api.eigentask.com

# --- Keycloak OIDC ---
KEYCLOAK_URL=https://auth.eigentask.com
KEYCLOAK_REALM=eigentask
KEYCLOAK_CLIENT_ID=eigentask
# If using a confidential client, set this
# KEYCLOAK_CLIENT_SECRET=replace-with-secret

# --- Callback URL ---
# Must be registered in the Keycloak client
# In dev:  http://localhost:8000/auth/callback
# In prod: https://api.eigentask.com/auth/callback
# CALLBACK_URL= https://api.eigentask.com/auth/callback

# --- Sessions/Cookies ---
SESSION_SECRET=replace-with-strong-random
COOKIE_DOMAIN=.eigentask.com
COOKIE_SECURE=true
SESSION_TTL_SECONDS=604800
SESSION_PREFIX=sess:

# --- Redis & Database ---
# In compose, other services are reachable by service name
REDIS_URL=redis://redis:6379/0
DATABASE_URL=postgresql+asyncpg://eigentask:change-me-strong@app-db:5432/eigentask

# --- Environment ---
ENVIRONMENT=production
