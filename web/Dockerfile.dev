# Web (SvelteKit) – development Dockerfile
FROM node:20-bullseye-slim

WORKDIR /app

# Copy dependency files first (for cache)
COPY package*.json ./

# Install *dev* dependencies
RUN npm install

# Mount your code in docker-compose, so don't COPY . here

# Expose the dev server port
EXPOSE 3000

# Start SvelteKit in dev mode with hot reload
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
