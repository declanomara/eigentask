# Example environment for Web (SvelteKit SSR)
# Copy this to your server as /etc/eigentask/web.env (do not commit real secrets)

# Base URL that the SSR server will use to call your API
# In compose networking, the API service is available at http://api:8000
# In production behind a reverse proxy, you can set this to https://api.eigentask.com
API_BASE=http://api:8000

# Node environment
NODE_ENV=production
