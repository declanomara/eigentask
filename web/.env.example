# Example environment for Web (SvelteKit SSR)
# Copy this to your server as /etc/eigentask/web.env (do not commit real secrets)

# Server-only origin the SSR will use to call your API (read via $env/dynamic/private)
# In compose networking, use the service name; in prod, your public API origin.
API_ORIGIN=http://api:8000

# Public origins exposed to the browser (read via $env/dynamic/public)
# These are safe to expose and help construct client-side links.
PUBLIC_API_ORIGIN=http://api:8000
PUBLIC_APP_ORIGIN=http://localhost:3000

# Node environment
NODE_ENV=production
